c++14：
OB表示二进制字面量，如OB1011表示二进制1011（十进制11）

std::exchange(x, 20);将x的值替换成20，返回x的旧值

==============================================================================================
c++20：
ranges::sort(arr);排序，支持容器和普通数组	非20：sort(arr, arr+arr.size());普通数组排序 sort(arr.begin(), arr.end());容器排序

ranges::lower_bound(a, b);在a里查找第一个不小于b的元素的迭代器

