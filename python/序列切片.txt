list = [ 0, 1, 2, 3, 4, 5, 6 ]

list[ 1 : 4 ]取出列表1-4之间的元素（不包括4），返回一个新列表，默认步长1

list[ : :2 ] 起始结束不写，默认从头到尾，步长为2

list[ 3 : 1 : -1 ]从后向前取

list[ : : -1 ][ 2 : 4] 先倒序切片，再切片

向按,分割成多个字符串，再通过下标指定字符串，再替换字符串内容，再倒序
str.split(",")[ 1 ].replace("来","")[ : : -1 ]